FROM python:3.11-slim

WORKDIR /app

# Install Python dependencies
RUN pip install --no-cache-dir flask flask-cors requests cryptography

COPY webhook_handler.py /app/

EXPOSE 5002

CMD ["python", "webhook_handler.py"]
